model_id: "answerdotai/ModernBERT-large"
revision: "main"
task_type: "text-generation"
dtype: "float32"

inputs:
  prompts:
    - "Here is my poem:"

level: "L4+L5"

generation:
  max_new_tokens: 20
  do_sample: false

skip_reason: "ModernBERT-large is primarily an encoder model; decoder head FP accumulation differences (atol=0.05)."
notes: "ModernBERT Large with decoder head. 3-component LM head: dense → LayerNorm → decoder. Separate Q/K/V projections."
