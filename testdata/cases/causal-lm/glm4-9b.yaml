model_id: "THUDM/glm-4-9b-chat-hf"
revision: "main"
task_type: "text-generation"
dtype: "float32"

inputs:
  prompts:
    - "Here is my poem:"

level: "L4+L5"

generation:
  max_new_tokens: 20
  do_sample: false

skip_reason: "GLM4 shares model_id with GLM (model_type=glm). 4-norm decoder variant not separately testable via HF."
notes: "GLM4 (model_type=glm4). 4-norm decoder (pre+post on both attention and MLP)."
