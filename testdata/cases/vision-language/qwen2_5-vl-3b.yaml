model_id: "Qwen/Qwen2.5-VL-3B-Instruct"
revision: "66285546d2b821cf421d4f5eb2576359d3770cd3"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "Describe this image in detail."
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

# The VL 3-model pipeline (vision encoder → embedding → decoder) accumulates
# float32 rounding differences vs HuggingFace PyTorch over autoregressive decode
# steps. First ~8 tokens match exactly; further tokens may diverge when two
# candidates are nearly equal in probability.
min_token_match_ratio: 0.25

notes: "Qwen2.5-VL 3B. Vision encoder + embedding + decoder pipeline."
