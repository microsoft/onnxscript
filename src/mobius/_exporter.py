# Copyright (c) ONNX Project Contributors
# SPDX-License-Identifier: Apache-2.0

"""Removed — import from the real modules instead.

The implementation lives in:

- :mod:`._builder` — ``build``, ``build_from_module``, ``resolve_dtype``, ``DTYPE_MAP``
- :mod:`._registry` — ``ModelRegistry``, ``ModelRegistration``, ``registry``
- :mod:`._config_resolver` — ``_config_from_hf``, ``_default_task_for_model``
- :mod:`._weight_loading` — ``apply_weights``, ``_download_weights``
- :mod:`._diffusers_builder` — ``build_diffusers_pipeline``
"""
